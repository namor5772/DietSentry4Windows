<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DietSentry"
             x:Class="DietSentry.EditRecipePage">

    <Grid>
        <ScrollView>
            <VerticalStackLayout
                Padding="20"
                Spacing="16">
                <Grid
                    RowDefinitions="{OnPlatform Android='Auto,Auto', Default='Auto'}"
                    ColumnDefinitions="Auto,*,Auto"
                    ColumnSpacing="12">
                    <Button
                        Grid.Column="0"
                        Text="?"
                        Style="{StaticResource HelpIconButtonStyle}"
                        Clicked="OnHelpClicked" />
                    <Label
                        Grid.Column="1"
                        Text="Edit Recipe"
                        FontAttributes="Bold"
                        FontSize="24"
                        VerticalOptions="Center" />
                    <Button
                        Grid.Column="{OnPlatform Android=0, Default=2}"
                        Grid.ColumnSpan="{OnPlatform Android=3, Default=1}"
                        Grid.Row="{OnPlatform Android=1, Default=0}"
                        Text="Back to Foods Table"
                        BackgroundColor="{StaticResource PrimaryDark}"
                        TextColor="{StaticResource White}"
                        WidthRequest="{OnPlatform Android=89.6, Default=-1}"
                        Clicked="OnBackClicked" />
                </Grid>

                <Entry
                    Placeholder="Recipe description" />

                <Editor
                    Placeholder="Ingredient list (placeholder)"
                    AutoSize="TextChanges"
                    HeightRequest="160" />

            <Button
                Text="Confirm"
                Clicked="OnConfirmClicked" />

        </VerticalStackLayout>
    </ScrollView>

        <Grid
            x:Name="HelpOverlay"
            IsVisible="False"
            BackgroundColor="#80000000"
            RowDefinitions="*,Auto">
            <BoxView
                Grid.Row="0"
                BackgroundColor="Transparent">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnHelpDismissed" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Border
                x:Name="HelpSheet"
                Grid.Row="1"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}"
                Padding="20,16"
                StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16,16,0,0" />
                </Border.StrokeShape>
                <Grid RowDefinitions="Auto,*" RowSpacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label
                            Text="{x:Static local:HelpContent.EditRecipeTitle}"
                            FontAttributes="Bold"
                            FontSize="18"
                            VerticalOptions="Center" />
                        <Button
                            Grid.Column="1"
                            Text="Close"
                            Clicked="OnHelpDismissed"
                            Padding="6,2"
                            FontSize="12"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
                    </Grid>
                    <local:MarkdownView
                        Grid.Row="1"
                        MarkdownText="{x:Static local:HelpContent.EditRecipeBody}"
                        VerticalOptions="FillAndExpand" />
                </Grid>
            </Border>
        </Grid>
    </Grid>

</ContentPage>
