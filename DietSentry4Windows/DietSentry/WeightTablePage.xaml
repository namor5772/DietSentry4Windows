<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DietSentry"
             x:Class="DietSentry.WeightTablePage"
             x:Name="WeightPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:NumericFormatConverter x:Key="NumericFormatConverter" />
            <Style
                x:Key="TopRowButtonStyle"
                TargetType="Button">
                <Setter
                    Property="Margin"
                    Value="0,0,4,4" />
                <Setter
                    Property="WidthRequest"
                    Value="{OnPlatform Android=89.6, Default=112}" />
            </Style>
            <Style
                x:Key="SelectionActionButtonStyle"
                TargetType="Button">
                <Setter
                    Property="Margin"
                    Value="0,0,4,4" />
                <Setter
                    Property="WidthRequest"
                    Value="79" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid
            Padding="20"
            RowDefinitions="Auto,*,Auto"
            RowSpacing="16">
            <Grid
                Grid.Row="0"
                RowDefinitions="{OnPlatform Android='Auto,Auto', Default='Auto'}"
                ColumnDefinitions="Auto,Auto,*"
                ColumnSpacing="12">
                <Button
                    Grid.Column="0"
                    Text="?"
                    Style="{StaticResource HelpIconButtonStyle}"
                    Clicked="OnHelpClicked"
                    VerticalOptions="Start" />
                <Label
                    Grid.Column="1"
                    Text="Weight Table"
                    FontAttributes="Bold"
                    FontSize="24"
                    VerticalOptions="Start" />
                <FlexLayout
                    Grid.Column="{OnPlatform Android=0, Default=2}"
                    Grid.ColumnSpan="{OnPlatform Android=3, Default=1}"
                    Grid.Row="{OnPlatform Android=1, Default=0}"
                    Direction="Row"
                    Wrap="Wrap"
                    AlignItems="Start"
                    JustifyContent="Start">
                    <Button
                        Text="Foods Table"
                        HorizontalOptions="Start"
                        BackgroundColor="{StaticResource PrimaryDark}"
                        TextColor="{StaticResource White}"
                        Clicked="OnBackClicked"
                        Style="{StaticResource TopRowButtonStyle}" />
                    <Button
                        Text="Add Weight"
                        HorizontalOptions="Start"
                        Clicked="OnAddClicked"
                        Style="{StaticResource TopRowButtonStyle}" />
                </FlexLayout>
            </Grid>

            <Frame
                Grid.Row="1"
                BorderColor="LightGray"
                Padding="10">
                <CollectionView
                    x:Name="WeightCollectionView"
                    ItemsSource="{Binding WeightEntries}"
                    ItemSizingStrategy="MeasureAllItems"
                    SelectionMode="Single"
                    SelectionChanged="OnWeightSelectionChanged">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout
                            Orientation="Vertical"
                            ItemSpacing="4" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.EmptyView>
                        <VerticalStackLayout Spacing="8">
                            <Label
                                Text="No weight entries yet." />
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame
                                BorderColor="LightGray"
                                Padding="6"
                                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                                InputTransparent="{OnPlatform Android=True, Default=False}">
                                <Grid
                                    ColumnDefinitions="Auto,Auto,*"
                                    ColumnSpacing="6"
                                    MinimumHeightRequest="24">
                                    <Label
                                        Grid.Row="0"
                                        x:Name="DateLabel"
                                        Text="{Binding DateWeight}"
                                        TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                                        VerticalOptions="Start" />
                                    <Label
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                                        HorizontalTextAlignment="End"
                                        VerticalOptions="Start"
                                        LineBreakMode="TailTruncation">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding Weight, Converter={StaticResource NumericFormatConverter}}" />
                                                <Span Text=" kg" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <Label
                                        Grid.Row="0"
                                        Grid.Column="2"
                                        Text="{Binding Comments}"
                                        TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}"
                                        VerticalOptions="Start"
                                        LineBreakMode="WordWrap" />
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Frame>

            <Frame
                Grid.Row="2"
                BorderColor="LightGray"
                Padding="10"
                IsVisible="{Binding ShowSelectionPanel}">
                <VerticalStackLayout Spacing="8">
                    <HorizontalStackLayout
                        HorizontalOptions="Center"
                        Spacing="8">
                        <Label
                            Text="{Binding SelectedWeightDateDisplay}"
                            FontAttributes="Bold" />
                        <Label
                            Text="{Binding SelectedWeightValueDisplay}" />
                    </HorizontalStackLayout>
                    <HorizontalStackLayout
                        HorizontalOptions="Center"
                        Spacing="0">
                        <Button
                            Text="Edit"
                            Clicked="OnEditSelectedClicked"
                            Style="{StaticResource SelectionActionButtonStyle}" />
                        <Button
                            Text="Delete"
                            Clicked="OnDeleteSelectedClicked"
                            Style="{StaticResource SelectionActionButtonStyle}" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <Grid
            x:Name="HelpOverlay"
            IsVisible="False"
            ZIndex="{OnPlatform Android=0, Default=30}"
            BackgroundColor="Transparent"
            RowDefinitions="*,Auto">
            <BoxView
                Grid.Row="0"
                BackgroundColor="Transparent"
                Color="#80000000">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnHelpDismissed" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Border
                x:Name="HelpSheet"
                Grid.Row="1"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}"
                Padding="20,16"
                StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16,16,0,0" />
                </Border.StrokeShape>
                <Grid RowDefinitions="Auto,*" RowSpacing="12">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label
                            Text="{x:Static local:HelpContent.WeightTableTitle}"
                            FontAttributes="Bold"
                            FontSize="18"
                            VerticalOptions="Center" />
                        <Button
                            Grid.Column="1"
                            Text="Close"
                            Clicked="OnHelpDismissed"
                            Padding="6,2"
                            FontSize="12"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
                    </Grid>
                    <local:MarkdownView
                        Grid.Row="1"
                        MarkdownText="{x:Static local:HelpContent.WeightTableBody}"
                        VerticalOptions="FillAndExpand" />
                </Grid>
            </Border>
        </Grid>

        <Grid
            BackgroundColor="Transparent"
            Grid.Row="0"
            IsVisible="{Binding ShowAddPanel}"
            ZIndex="{OnPlatform Android=0, Default=10}">
            <BoxView
                BackgroundColor="Transparent"
                Color="#80000000">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnAddBackdropTapped" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Frame
                Padding="16"
                CornerRadius="12"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="{OnPlatform Android=291.2, Default=364}"
                HasShadow="True">
                <Frame.Shadow>
                    <Shadow
                        Brush="#40000000"
                        Offset="0,8"
                        Radius="12"
                        Opacity="0.6" />
                </Frame.Shadow>
                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="Add Weight table record"
                        FontAttributes="Bold"
                        FontSize="18"
                        HorizontalTextAlignment="Center" />
                    <Grid
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="8">
                        <DatePicker
                            x:Name="AddDatePicker"
                            VerticalOptions="Center"
                            Grid.Column="0" />
                        <Entry
                            x:Name="AddWeightEntry"
                            Placeholder="Weight (kg)"
                            Keyboard="Numeric"
                            HorizontalOptions="FillAndExpand"
                            Grid.Column="1" />
                    </Grid>
                    <Editor
                        x:Name="AddCommentsEditor"
                        Placeholder="Comments"
                        AutoSize="TextChanges" />
                    <FlexLayout
                        Direction="Row"
                        Wrap="Wrap"
                        AlignItems="Center"
                        JustifyContent="Center">
                        <Button
                            Text="Confirm"
                            Clicked="OnAddConfirmClicked"
                            Margin="0,0,4,4" />
                        <Button
                            Text="Cancel"
                            Clicked="OnAddCancelClicked"
                            Margin="0,0,4,4" />
                    </FlexLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <Grid
            BackgroundColor="Transparent"
            Grid.Row="0"
            IsVisible="{Binding ShowEditPanel}"
            ZIndex="{OnPlatform Android=0, Default=10}">
            <BoxView
                BackgroundColor="Transparent"
                Color="#80000000">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnEditBackdropTapped" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Frame
                Padding="16"
                CornerRadius="12"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="{OnPlatform Android=291.2, Default=364}"
                HasShadow="True">
                <Frame.Shadow>
                    <Shadow
                        Brush="#40000000"
                        Offset="0,8"
                        Radius="12"
                        Opacity="0.6" />
                </Frame.Shadow>
                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="Edit Weight table record"
                        FontAttributes="Bold"
                        FontSize="18"
                        HorizontalTextAlignment="Center" />
                    <Grid
                        ColumnDefinitions="Auto,*"
                        ColumnSpacing="8">
                        <Label
                            x:Name="EditDateLabel"
                            VerticalOptions="Center"
                            FontAttributes="Bold"
                            Grid.Column="0" />
                        <Entry
                            x:Name="EditWeightEntry"
                            Placeholder="Weight (kg)"
                            Keyboard="Numeric"
                            HorizontalOptions="FillAndExpand"
                            Grid.Column="1" />
                    </Grid>
                    <Editor
                        x:Name="EditCommentsEditor"
                        Placeholder="Comments"
                        AutoSize="TextChanges" />
                    <FlexLayout
                        Direction="Row"
                        Wrap="Wrap"
                        AlignItems="Center"
                        JustifyContent="Center">
                        <Button
                            Text="Confirm"
                            Clicked="OnEditConfirmClicked"
                            Margin="0,0,4,4" />
                        <Button
                            Text="Cancel"
                            Clicked="OnEditCancelClicked"
                            Margin="0,0,4,4" />
                    </FlexLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <Grid
            BackgroundColor="Transparent"
            Grid.Row="0"
            IsVisible="{Binding ShowDeletePanel}"
            ZIndex="{OnPlatform Android=0, Default=10}">
            <BoxView
                BackgroundColor="Transparent"
                Color="#80000000">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnDeleteBackdropTapped" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Frame
                Padding="16"
                CornerRadius="12"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="{OnPlatform Android=291.2, Default=364}"
                HasShadow="True">
                <Frame.Shadow>
                    <Shadow
                        Brush="#40000000"
                        Offset="0,8"
                        Radius="12"
                        Opacity="0.6" />
                </Frame.Shadow>
                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="Delete Weight table record?"
                        FontAttributes="Bold"
                        FontSize="18"
                        HorizontalTextAlignment="Center" />
                    <Label
                        x:Name="DeleteDetailsLabel"
                        HorizontalTextAlignment="Center" />
                    <FlexLayout
                        Direction="Row"
                        Wrap="Wrap"
                        AlignItems="Center"
                        JustifyContent="Center">
                        <Button
                            Text="Confirm"
                            Clicked="OnDeleteConfirmClicked"
                            Margin="0,0,4,4" />
                        <Button
                            Text="Cancel"
                            Clicked="OnDeleteCancelClicked"
                            Margin="0,0,4,4" />
                    </FlexLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <Grid
            BackgroundColor="Transparent"
            x:Name="InvalidWeightOverlay"
            IsVisible="False"
            ZIndex="{OnPlatform Android=0, Default=20}">
            <BoxView
                BackgroundColor="Transparent"
                Color="#80000000">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnInvalidWeightBackdropTapped" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Frame
                Padding="16"
                CornerRadius="12"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="{OnPlatform Android=256, Default=320}"
                HasShadow="True">
                <Frame.Shadow>
                    <Shadow
                        Brush="#40000000"
                        Offset="0,8"
                        Radius="12"
                        Opacity="0.6" />
                </Frame.Shadow>
                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="Invalid weight"
                        FontAttributes="Bold"
                        FontSize="18"
                        HorizontalTextAlignment="Center" />
                    <Label
                        x:Name="InvalidWeightMessageLabel"
                        HorizontalTextAlignment="Center" />
                    <FlexLayout
                        Direction="Row"
                        Wrap="Wrap"
                        AlignItems="Center"
                        JustifyContent="Center">
                        <Button
                            Text="OK"
                            Clicked="OnInvalidWeightOkClicked"
                            Margin="0,0,4,4" />
                    </FlexLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <Grid
            BackgroundColor="Transparent"
            x:Name="DateExistsOverlay"
            IsVisible="False"
            ZIndex="{OnPlatform Android=0, Default=20}">
            <BoxView
                BackgroundColor="Transparent"
                Color="#80000000">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnDateExistsBackdropTapped" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <Frame
                Padding="16"
                CornerRadius="12"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="{OnPlatform Android=256, Default=320}"
                HasShadow="True">
                <Frame.Shadow>
                    <Shadow
                        Brush="#40000000"
                        Offset="0,8"
                        Radius="12"
                        Opacity="0.6" />
                </Frame.Shadow>
                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="Date exists"
                        FontAttributes="Bold"
                        FontSize="18"
                        HorizontalTextAlignment="Center" />
                    <Label
                        x:Name="DateExistsMessageLabel"
                        HorizontalTextAlignment="Center" />
                    <FlexLayout
                        Direction="Row"
                        Wrap="Wrap"
                        AlignItems="Center"
                        JustifyContent="Center">
                        <Button
                            Text="OK"
                            Clicked="OnDateExistsOkClicked"
                            Margin="0,0,4,4" />
                    </FlexLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>

    </Grid>

</ContentPage>
